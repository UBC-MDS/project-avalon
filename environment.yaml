## Setup environment for the first time:
#   - switch the project root directory
#   - Run:
#       `conda env create --name avalon --file=environment.yaml`
# Update environment:
#   - If you need the avalon env locally, run 
#       `conda activate avalon`
#   - If you added a new package in your local environment.yaml, you need to run 
#       `conda env update --name avalon --file=environment.yaml`
# How to use:
#   - Switch to avalon env by clicking the drop-down in jupyter notebook, and then select the `avalon` kernel
##

name: 522-project-avalon
channels:
  - conda-forge
  - defaults
dependencies:
  - altair=5.1.2
  - ipykernel=6.26.0
  - matplotlib=3.8.1
  - nb_conda_kernels=2.3.1
  - pip=23.3.1
  - python=3.11.6
  - scikit-learn=1.3.2
  - scipy=1.11.3
  - vl-convert-python=1.1.0
  - vegafusion=1.4.3
  - vegafusion-python-embed=1.4.3
  - vegafusion-jupyter=1.4.3
  - vega_datasets=0.9.0
  - folium=0.15.0
  - seaborn=0.13.0
prefix: /Users/benchen/miniconda3/envs/522-project-avalon

 # Notes:
 # - matplotlib  # if you require more traditional types of plots or highly customized visualizations that Altair does not support
 # - panel #If you plan to create interactive dashboards or web applications that incorporate your Altair visualizations

